<!DOCTYPE html>
<html>
  <head>
    <title>RentADress</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Ubuntu:wght@300&display=swap" rel="stylesheet">
  </head>

  <body>
    <div class="navbar navbar-expand bg-white">
      <%=link_to root_path do%>
        <%=image_tag 'dress.svg', class:"icon-navbar"%>
      <%end%>

      <div class="navbar-right">
        <% if current_user == nil %>
          <a class='out pe-3' href="<%=  new_user_registration_path %>">Sign up</a>
          <a class='out pe-3' href="<%= new_user_session_path %>">Log in</a>
        <% else %>
          <%=link_to user_path(current_user) do%>
            <%=image_tag 'user_icon.svg', class:"user_icon"%>
          <%end%>
          <%= button_to(
          "Log Out",
          destroy_user_session_path,
          method: :delete,
          class: "btn btn-link"
          ) %>
        <%end%>
      </div>
    </div>

    <%= yield %>

    <div class="footer">
      <div class="footer-links">
        <a href="#"><i class="fab fa-instagram"></i></a>
        <a href="#"><i class="fab fa-facebook"></i></a>
        <a href="#"><i class="fab fa-twitter"></i></a>
      </div>
      <div class="footer-copyright">
        This footer is made by the Green Thumb team
      </div>
    </div>


  </body>
</html>
